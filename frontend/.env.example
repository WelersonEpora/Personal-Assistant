# URL base da API. Vazia em dev (usa o proxy do Vite para o backend local,
# ver vite.config.js) e em produção quando frontend e backend ficam atrás do
# mesmo domínio (nginx do frontend faz proxy interno de /api e /health).
VITE_API_BASE_URL=
